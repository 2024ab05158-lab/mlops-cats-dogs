name: CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t cats-dogs-api .

      - name: Smoke Test (simulated)
        run: echo "CD pipeline executed"
